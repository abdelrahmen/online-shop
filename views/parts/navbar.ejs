<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">lambda</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="Home" href="/">Home</a>
        </li>
        <% if (isUser) { %>

        <li class="nav-item">
          <a class="nav-link active" aria-current="Cart" href="/cart">Cart</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="Orders" href="/orders"
            >Orders</a
          >
        </li>
        <% }else{ %>
        <li class="nav-item">
          <a class="nav-link active" aria-current="Login" href="/login">Login</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="signup" href="/signup"
            >Sign Up</a
          >
        </li>
        <% } %>
        <% if (isAdmin) { %>
          <li class="nav-item">
            <a class="nav-link active" aria-current="Add Products" href="/admin/add"
              >Add Products</a
            >
          </li>
          <li class="nav-item">
            <a class="nav-link active" aria-current="manage orders" href="/admin/orders"
              >manage orders</a
            >
          </li>
        <% } %>
      </ul>
      <% if (isUser) { %>
      <form action="/logout" method="post">
        <input type="submit" value="Logout" class="btn btn-danger" />
      </form>
      <% } %>
    </div>
  </div>
</nav>
